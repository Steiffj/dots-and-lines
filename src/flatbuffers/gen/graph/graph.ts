// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Attributes } from '../graph/attributes.js';
import { Edge } from '../graph/edge.js';
import { Node } from '../graph/node.js';


export class Graph {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Graph {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGraph(bb:flatbuffers.ByteBuffer, obj?:Graph):Graph {
  return (obj || new Graph()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGraph(bb:flatbuffers.ByteBuffer, obj?:Graph):Graph {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Graph()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

attributes(obj?:Attributes):Attributes|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Attributes()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

nodes(index: number, obj?:Node):Node|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Node()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

nodesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

edges(index: number, obj?:Edge):Edge|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Edge()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

edgesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startGraph(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addAttributes(builder:flatbuffers.Builder, attributesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, attributesOffset, 0);
}

static addNodes(builder:flatbuffers.Builder, nodesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nodesOffset, 0);
}

static createNodesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startNodesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEdges(builder:flatbuffers.Builder, edgesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, edgesOffset, 0);
}

static createEdgesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEdgesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endGraph(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishGraphBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedGraphBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}

static createGraph(builder:flatbuffers.Builder, attributesOffset:flatbuffers.Offset, nodesOffset:flatbuffers.Offset, edgesOffset:flatbuffers.Offset):flatbuffers.Offset {
  Graph.startGraph(builder);
  Graph.addAttributes(builder, attributesOffset);
  Graph.addNodes(builder, nodesOffset);
  Graph.addEdges(builder, edgesOffset);
  return Graph.endGraph(builder);
}
}
